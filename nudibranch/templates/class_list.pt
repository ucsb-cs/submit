<div metal:use-macro="_LAYOUT">
<div metal:fill-slot="content">
  <p tal:condition="request.user.is_admin">
    <a href="${_R('class_new_form')}">Create new class</a>
  </p>
  <div tal:repeat="klass classes">
    <a href="${_R('class_item', class_name=klass.name)}">${klass.name}</a>
  </div>
</div>
</div>
